# flutter_audio_recorder 鸿蒙化需求说明书（SRS）

## 1 需求概述

### 1.1 需求来源与场景

- 需求来源：自研鸿蒙化。
- 需求场景：`flutter_audio_recorder` 是一个跨平台 Flutter 录音插件，原有能力覆盖 Android/iOS。随着 HarmonyOS 生态中跨平台应用数量增长，开发者需要在保持 Flutter 调用接口一致的前提下，在鸿蒙设备上实现录音初始化、开始/暂停/恢复/停止、录音状态查询与音频电平计量等能力，以降低业务侧多平台适配成本并缩短上架周期。
- 目标价值：形成可复用的鸿蒙录音插件能力，支持应用快速接入并稳定运行。

### 1.2 需求简介

将 `flutter_audio_recorder` 迁移并稳定运行于 HarmonyOS，保持 Dart 层 API 基本不变，实现鸿蒙侧录音核心能力、权限交互与异常处理闭环，面向 Flutter 应用开发者提供可直接集成的录音插件。

### 1.3 需求限制及范围

- IDE 版本：DevEco Studio 5.0.0+。
- API Level：API 12（HarmonyOS 5.0.0）起步。
- 约束说明：根据项目现状与技能规则，最低版本统一不低于 API 12。

功能范围：
- ✅ 覆盖范围：
  - 麦克风权限申请与授权状态判断
  - 录音初始化、开始、暂停、恢复、停止
  - 录音状态与计量数据查询
  - `.wav/.m4a/.mp4/.aac` 格式支持（受 API/容器能力约束）
  - 权限拒绝后二次授权流程
- ❌ 暂不覆盖：
  - 云端转写、降噪、音频后处理算法增强
  - 插件外的业务 UI 组件标准化
  - 多进程录音并发场景

## 2 需求分析

### 2.1 竞品分析

竞品基线定义：
- 竞品对象为鸿蒙化前原始平台能力（Android/iOS 录音插件实现）及同类热门录音插件。

原始库与鸿蒙化版本对比：

| 对比项 | 原始库（Android/iOS） | 鸿蒙化版本（OHOS） | 说明 |
|--------|------------------------|---------------------|------|
| 调用接口 | Flutter MethodChannel | Flutter MethodChannel | 保持一致 |
| 录音能力 | 已具备 | 已具备 | 功能对齐 |
| 权限流程 | 运行时权限申请 | 运行时 + 二次授权 | 鸿蒙侧做了增强 |
| 构建体系 | Gradle/CocoaPods | hvigor + HAR | 工程体系差异 |

社区活跃度（数据仅供参考）：

| 指标 | 说明 |
|------|------|
| GitHub Star | 原仓库存在历史活跃度，但该值可能随时间变化（建议实施前复核） |
| 更新时间 | 以仓库提交时间为准（建议实施前复核） |
| 归档状态 | 未发现明确归档信息（建议实施前复核） |

可替代库列表（仅供参考）：

| 仓库名称 | 仓库链接 | 功能介绍 |
|----------|----------|----------|
| `record` | https://github.com/llfbandit/record | Flutter 多平台录音插件，能力较全 |
| `flutter_sound` | https://github.com/Canardoux/flutter_sound | 录放音一体，功能覆盖广 |
| `audio_recorder2` | https://github.com/rmbrone/audio_recorder2 | 轻量录音插件方案 |
| `just_audio`（录音需配套） | https://github.com/ryanheise/just_audio | 播放为主，需组合录音能力 |
| `audioplayers`（录音需配套） | https://github.com/bluefireteam/audioplayers | 播放为主，常与录音插件组合 |

### 2.2 用户研究

目标用户群体：
- HarmonyOS Flutter 应用开发者
- 需要语音采集能力的业务开发团队
- 维护多平台统一代码基线的中台/SDK 团队

核心诉求：
- 接口一致、接入成本低
- 权限流程稳定可控
- 录音结果可预期、格式兼容清晰

#### 2.2.1 KANO 模型分析

| 特性 | KANO 类型 | 说明 |
|------|-----------|------|
| 基础录音可用（start/stop） | 基本型 | 不可缺失 |
| 权限申请成功率与可恢复性 | 基本型 | 拒绝后需可恢复 |
| API 与 Android/iOS 一致 | 期望型 | 降低迁移成本 |
| 暂停/恢复能力 | 期望型 | 提升业务灵活性 |
| 多格式容器支持 | 期望型 | 满足不同业务存储策略 |
| 电平计量数据可读 | 兴奋型 | 提升交互体验 |
| 二次授权智能触发 | 兴奋型 | 降低用户流失 |
| 详细错误码与日志 | 期望型 | 提升排障效率 |
| 过度复杂的高级音频后处理 | 无差异型 | 非当前核心需求 |
| 强制企业能力集成 | 反向型 | 普通应用不需要 |

#### 2.2.2 关键体验指标

| 指标 | 用户期望 | 方案设计关注点 |
|------|----------|----------------|
| API 易用性 | 接入步骤少、学习成本低 | 保持 Dart API 不变，减少破坏性改动 |
| 功能完整性 | Android/iOS 能力在鸿蒙可用 | 核心生命周期与权限能力对齐 |
| 性能表现 | 启动快、录音稳定 | 录音链路无卡顿、长录音资源可控 |
| 文档完善度 | 快速排障、快速上线 | 提供兼容矩阵、FAQ、错误处理说明 |

#### 2.2.3 用户历程图

| 场景编号 | 类型 | 描述 | 用户行为 | 用户情绪 |
|----------|------|------|----------|----------|
| UJ-01 | ✅ 正向 | 首次接入成功录音 | 按文档接入并调用 init/start | 满意 |
| UJ-02 | ✅ 正向 | 权限允许后持续录音 | 授权后正常录制并查询状态 | 稳定 |
| UJ-03 | ✅ 正向 | 暂停恢复后成功停止 | pause/resume/stop 正常工作 | 信任 |
| UJ-04 | ❌ 反向 | 首次拒绝权限后无法恢复 | 多次点击无授权弹窗 | 挫败 |
| UJ-05 | ❌ 反向 | 格式不兼容导致初始化失败 | 使用不支持容器/版本 | 困惑 |
| UJ-06 | ❌ 反向 | 依赖版本漂移导致构建失败 | 拉取新依赖后编译异常 | 焦虑 |

### 2.3 商业模式与收益分析

不涉及。

### 2.4 需求依赖分析

| 依赖类别 | 约束内容 | 对方案影响 |
|----------|----------|------------|
| Flutter 运行时 | 依赖 `@ohos/flutter_ohos` | 必须保持插件桥接兼容 |
| 音频能力 | 依赖 `@kit.AudioKit`/`@kit.MediaKit` | 录音格式与采样能力需适配 |
| 权限能力 | 依赖 `@kit.AbilityKit` | 授权时序是关键稳定点 |
| 文件能力 | 依赖 `@kit.CoreFileKit` | 临时文件与持久化状态管理 |
| 构建体系 | hvigor/oh-package | 需维持 HAR 产线稳定 |

### 2.5 运营需求

不涉及。

### 2.6 法务与合规

不涉及。

### 2.7 特性关键体验指标量化设计

| 指标 | 量化目标 | 测量方法 |
|------|----------|----------|
| API 易用性 | 核心接入步骤 ≤ 5 步 | 按接入文档执行验收 |
| 功能完整性 | 核心 API 覆盖率 ≥ 95% | 对照 Android/iOS API 清单 |
| 性能表现 | `init -> start` P95 ≤ 300ms（示例基线） | 真机压测与日志采样 |
| 文档完善度 | 核心场景文档覆盖率 100% | 文档检查清单评审 |

### 2.8 其他分析

不涉及。

## 3 需求描述

### 3.1 功能需求

#### 3.1.1 关键体验路径识别

| 路径序号 | 路径名称 | 描述 | 涉及功能点 | 关键体验指标 | 用户期望 | 设计关注点 |
|----------|----------|------|------------|--------------|----------|------------|
| P-01 | 插件接入路径 | 添加依赖并完成初始化调用 | pub 依赖、initialized | API 易用性 | 快速接入 | 示例与文档一致 |
| P-02 | 正常录音路径 | hasPermissions -> init -> start -> stop | 权限、录音生命周期 | 功能完整性 | 稳定可用 | 授权和状态机一致性 |
| P-03 | 中断恢复路径 | start -> pause -> resume -> stop | pause/resume | 稳定性 | 无异常中断 | 状态迁移与资源释放 |
| P-04 | 异常恢复路径 | 首次拒绝后再次点击 init 触发二次授权 | 权限二次授权 | 可恢复性 | 可自愈 | 状态持久化与时序控制 |
| P-05 | 排障路径 | 初始化失败或格式不支持时定位问题 | 错误码、日志 | 文档完善度 | 快速定位 | 错误语义清晰 |

#### 3.1.2 场景详细设计（5W1H1E）

场景 A：首次拒绝权限后恢复录音能力

| 维度 | 内容 |
|------|------|
| Who | Flutter 业务开发者、终端用户 |
| What | 用户首次拒绝麦克风权限后，需通过再次点击 Init 触发二次授权 |
| When | 首次授权拒绝后再次发起录音前 |
| Where | HarmonyOS 终端应用内 |
| Why | 避免一次拒绝导致功能永久不可用 |
| How | 记录拒绝状态，下一次权限检查时拉起系统设置二次授权 |
| How much | 增量改造约 1~2 人天（权限链路） |
| Effect | 权限恢复成功率提升，用户流失降低 |

场景 B：多格式录音稳定输出

| 维度 | 内容 |
|------|------|
| Who | 接入录音插件的业务开发者 |
| What | 在 `.wav/.m4a/.mp4/.aac` 下稳定初始化并录音 |
| When | 业务选择不同录音格式时 |
| Where | HarmonyOS 设备录音场景 |
| Why | 满足不同业务存储与兼容需求 |
| How | 按格式配置编码器参数并做 API Level 校验 |
| How much | 增量改造约 2~3 人天（兼容回归） |
| Effect | 初始化失败率下降，格式行为可预期 |

#### 3.1.3 原型/交互稿

不涉及。

#### 3.1.4 文案与多语言

不涉及。

### 3.2 其他非功能设计

兼容性：
- 目标系统：HarmonyOS API 12 及以上。
- 设备类型：手机/平板（以实际模块配置为准）。

安全性：
- 权限最小化：仅申请录音所需权限。
- 敏感数据：录音文件存储在应用沙箱目录内，按业务要求进行生命周期管理。

性能：
- 录音初始化、状态轮询、文件写入需满足长时录音稳定性要求。
- 建议监控内存占用、fd 使用与写盘耗时。

DFX：
- 日志分级：`info/warn/error`。
- 错误码：初始化失败、权限失败、状态非法、格式不支持需具备可追踪错误语义。

### 3.3 附录与其他说明

不涉及。

